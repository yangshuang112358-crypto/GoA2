# 稳定基线与实验回退（长期保留）

2026-09-20用户要求：后续可能降低AI档位做质量实验，必须能恢复到实验前当前进度。本文件与AGENTS.md共同作为项目长期记忆，不因上下文压缩或更换模型而失效。

固定入口：`baseline/2026-09-20-engine34`（附注标签），`baseline/engine34-20260920`（保留分支）。二者指向本次基线记录提交；游戏源码与其父提交8413aa8相同，上一张规则实现为3a89ed3。禁止移动/重建标签、删除保留分支或强推覆盖基线。

冻结内容：引擎34，6名英雄、108张数据、45张已实现，历史完整回归963项C#与32项Python通过，55个手工预设，33个冻结存档/65个校验文件。历史验收见card-batch-engine34.json；不把历史测试描述成2026-09-20刚重新执行。

固定Windows包：`artifacts/releases/Goa2V1-e34-closesupport-20260912-215311-458129db.zip`，SHA-256 `0691ed5786d25825e23a71d7ecc1d53a87279d0ff1c509fe4ed80083c33615e4`。它不受后续artifacts/player重建影响，不得覆盖。Git不保存Unity缓存和发行ZIP，换电脑须另备份此包；源码标签可重新构建。另保存本地Git bundle于`artifacts/baselines/engine34-20260920.bundle`，用git bundle verify验证。

后续开发分支：`experiment/ai-level-20260920`。照常执行合同→失败测试→实现→专项/回归→实际Player→文档与小步提交。模型档位变化不降低证据要求，未完成的卡不能标为implemented。新发行包必须使用独立引擎/标签名称。

## 查看或试运行基线（保留当前实验）

在Goa2V1根目录PowerShell执行：

```powershell
git show --no-patch baseline/2026-09-20-engine34
git worktree add --detach ../Goa2V1-baseline-e34 baseline/2026-09-20-engine34
```

第二条使用尚不存在的目录建立独立源码副本，不修改实验工作区。运行现成基线可把固定ZIP解压到另一个目录后启动Goa2V1.exe。不要用新版存档覆盖基线的测试存档。

## 用户决定正式回退时

先用git status检查并保存实验修改；已有提交保留在experiment分支，未提交变更先精确提交或另存补丁及新增文件。工作区干净后：

```powershell
git switch -c recovery/engine34 baseline/2026-09-20-engine34
```

这会建立恢复分支并保留全部实验历史。若恢复分支已存在，换一个唯一名称。不要自动执行reset --hard、clean、强推或删除分支。是否把恢复版本合入main由用户的回退指令决定。

仅在远端或本地原始仓库丢失时使用bundle恢复到新目录；正常回退优先使用固定标签。标签、保留分支、实验分支均应推送原GitHub仓库。
