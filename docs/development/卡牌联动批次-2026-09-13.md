# 卡牌联动批次：偷袭与忠实信徒

2026-09-13，当前引擎12。新增两张完整牌文路径，正式牌效状态23/108（implemented），其余85张data_only。implemented表示有运行实现与针对性证据，不表示全部组合或大招已经测完。

| 卡牌 | 已实现 | 代表联动 |
|---|---|---|
| 虎爪·偷袭 | 相邻攻击后，本人选择普通移动1格或跳过；保存恢复；无路自动继续；牌文1格不吃移动图标加成 | 挑战者/带头冲锋防御；静电封锁；打断施法不阻止攻击；重型兵推进与待出生；胜利立即终止 |
| 夏尔加萨·忠实信徒 | 相邻任意阵营近战/远程/重型兵时，可取回本人一张弃牌或跳过；私有选择；保存恢复 | 取回劈砍后同回合防御拔枪；快速突刺被反射屏障迫使弃牌后，下回合取回；打断施法封锁；轮末不重复回收 |

偷袭的同步推线曾重复创建移动窗口，已由失败测试复现并修复。注册表声明卡片程序，共享执行器不按卡ID特判；牌文移动与取回保留父行动、选择权与版本信息。新增引擎10、11真实Player历史夹具；旧存档不自动获得新规则，仅在安全暗选阶段显式升级。

本批不包含网络服务、房间或多人客户端。六张紫卡文字仍未实现；已审查其行动前触发、基础类型、重复、免疫、穿越、推动和兵线替换风险，见[集中待裁定清单](../planning/卡牌联动与待裁定汇总.md)。魅惑因Q-01计算范围不明暂放，未开放半张移动效果。

自动测试：完整C# 488/488（478核心、10呈现），两张各21项专用用例；Python 32/32。原生场景结果及构建哈希补录至本页下方。没有测量代码行覆盖率。

物理输入边界：偷袭脚本启动后另一程序占用前台，安全检查中止，没有继续向其他窗口发送输入；忠实信徒物理脚本因此暂未执行。两份脚本已写，均不能计入本批通过项。17模块完整键鼠套件未全量重跑；旧引擎10反制21项物理输入仅是历史证据。隐藏窗口卡面几何检查与无窗口规则场景也不冒充物理点击验收。

全部命令在Goa2V1根目录的PowerShell执行：

~~~powershell
./tools/run-player.ps1
./tools/open-debug-position.ps1 -List
./tools/open-debug-position.ps1 -Name sneak-move
./tools/open-debug-position.ps1 -Name sneak-static
./tools/open-debug-position.ps1 -Name loyal-recover
./tools/open-debug-position.ps1 -Name loyal-defense
./tools/open-debug-position.ps1 -Name loyal-silence
# 仅生成并验证起点，不打开窗口
./tools/open-debug-position.ps1 -Name loyal-recover -PrepareOnly
# 无窗口场景；省略-Scenario运行全部38份
./tools/run-scenarios.ps1 -Scenario tests/scenarios/loyal-recover.json
# 可见、暂停、逐步播放
./tools/run-scenarios.ps1 -Scenario tests/scenarios/sneak-static.json -Visual -Paused -KeepOpen
./tools/test-core.ps1
# 前台可用且没有手工操作时，执行真实输入测试
./tools/test-ui-suite.ps1 -Case sneak,loyal
~~~

游戏内“调试→打开测试局面…”可搜索上述五个新增局面，目前共15个预设。偷袭移动在右栏选格确认或“不移动”；忠实信徒在右栏选弃牌确认或“不取回”；确认支持空格。

完整纯文本说明位于artifacts/运行命令与效果说明.txt，与player目录并列。包含所有可执行入口、前提与效果；不需要从手机点开Markdown链接才能找到。


## 本批最终验收

引擎12全部38份原生场景、557步通过；每份输入与五个当前程序集SHA-256一致，保存版本12、完整完成且退出0。108张卡在1280×800的真实Player几何/合成指针检查通过，非物理点击；离线浏览器23项检查通过。新5个调试预设另用PrepareOnly生成并验证，当前总15个。

两张各21项专用核心用例，完整C#488项、Python32项通过。忠实信徒失败先行17项中14项因缺少行为失败、3项既有路径通过；再实现并加入连招回归。对方屏障导致本人的弃牌、取回后的同回合防御、技能封锁、双方三类小兵、轮末回收、私有投影及旧引擎升级均通过。物理输入阻塞/未运行保持明确，不计通过。

机器可核对证据：[引擎12验收](../verification/card-batch-engine12.json)。本地测试包：artifacts/releases/Goa2V1-e12-cards-20260913-20260912-170136-92e31c8e.zip；SHA-256：64f526b965bf50a9a4ec48c99d0180b7dd2a0990e4aa4fc6dbdc63e3e1e64ce7。这个包仍是四席本地热座开发版，不是全牌或联网正式版。
