# BATCH-02 工作记录

2026-09-10 10:07开始，预计工作至约22:07（Asia/Shanghai）。起点commit 71f0268；31项核心和11项资料工具测试为上一批基线。

当前阶段：本批界面、测试模式、调试接口、代表牌和验收已完成，交付清单登记最终归档文件。逐项结果见[BATCH-02验收](BATCH-02验收.md)，原授权范围见[BATCH-02](../planning/BATCH-02.md)。以下阶段保留当时的事实与未完成状态，不把后续结果倒填为当时已经通过。

## 第一阶段：测试对局核心

新增6项SandboxTests，先在Release运行中证明调试命令/自动揭示尚未支持，再实施；当前完整核心37/37通过，结果artifacts/tests/core.trx。首次Debug构建被本机应用控制拒绝加载测试DLL，记录为环境失败；之后使用项目正常Release配置，没有修改系统安全设置。

已实现新局固定Sandbox标志、选完自动揭示/正式四确认切换、加减金币、合法格传送、弃牌/取回、自动选英雄与出生、第一张/最高先攻/可重放随机快选、同色测试装配、决策币。调试意图仍经过身份/版本/原子命令检查并写入回放；普通对局拒绝调试命令。

公开视图仅暴露弃牌颜色；本轮四回合出牌历史由揭示事件构成，卡牌之后离开已出区也不删除历史。原有正式确认存档已在新版Player实际读取成功。

资料校验仍为6英雄、108牌、254格、44障碍；11/11资料工具测试通过，正式内容和来源哈希保持不变。尚未提升任何牌效实现状态。

## 第二阶段：界面与真实输入验收（10:49）

UI-01至UI-06、DEBUG-02已实现。四周扩大并独立折叠、无交接屏幕、1—4快捷键、字体放大、按鼠标位置缩放/拖动/Home全图、四回合颜色槽与独立弃牌圆点、顶部揭示牌跨选牌阶段保留。滚动位置随刷新保留；1280×800下默认聚焦可辨认的战区，允许折叠面板看大地图。

tools/test-player-ui.ps1在1600×1000和1280×800各完成19项检查；输入全部通过Win32鼠标/键盘发送给真实前台Player，没有直接注入权威状态。报告artifacts/unity/ui-smoke-1600x1000.json与ui-smoke-1280x800.json，附两组discard/complete截图。覆盖四人快揭示、四边折叠、鼠标锚点缩放、拖动后切角色、数字编辑不切角色、缩放后点格/传送、弃牌/取回、下一回合公开牌保留。

Unity BuildWindows成功，实际Player无异常日志。新增run-player.ps1的QA模式将存档隔离到artifacts目录，保留正常用户存档；-goaLoad恢复指定文件。

仍在继续本批后续开发；上述检查不代表主要牌效或完整对局已经完成。

## 第三阶段：流程快进与Unity同源测试

新增DebugConfirmAll、DebugAdvance(action/turn/round)、DebugSetGold，以及5项边界测试。真实窗口点击“快速到轮末”后，修订2即完成自动准备与整轮快进，记录16次揭示、16次放弃后结算，停在RoundEnd；截图batch02-debug-roundend.png，实际存档qa-save.json。

新增规则最初4项失败均为unsupported_command；实现后.NET测试宿主被Windows Smart App Control拒绝加载Goa2.Rules.dll（0x800711C7）。同一二进制重试仍被拒，已核实系统CodeIntegrity事件；未修改安全策略，也没有把环境失败当作行为失败或通过。

已将核心测试移到UPM包Tests/Editor，.NET工程引用同样源码，接入编辑器自带Test Framework 1.6.0。Unity实际42/42通过；其中“快进不能丢弃无关强制选择”先取得0/1失败，再增加拒绝边界并通过。结果artifacts/unity/tests/core-editmode.xml。新增test-core.ps1的显式运行器选择与仅环境错误时的Auto切换。

Player重新构建与运行成功；测试/NUnit程序集没有进入发行Player。场景无窗口/可见执行仍在实施中。

随后在加入强制选择保护并重新编译后，.NET也实际42/42通过（最新core.trx）；此前两份被阻止的报告仍保留。说明拦截并非每次构建都会发生，不能因此抹去环境问题或宣称安全策略已改变。

## 第四阶段：无窗口与可见场景

QA-01已实现。ScenarioRunner由Unity无窗口Player和可见Player共用；9步沙盒、7步普通对局权限、23步正式确认场景均通过无窗口执行。可见沙盒已实测暂停起步、单步、阻止手工命令干扰、继续播放；正式确认场景也在窗口通过。两者分别与无窗口最终状态哈希完全一致。

- sandbox-smoke：40e5a974939ba7f1e2485a5a33b991b4e90d5233bbafd6c52fad2c590aa0b10b。
- formal-turn：b1854c814e65f3095aeda47072b220d218d7878ec748b95f291c0d697325e587。
- permissions：e473acdbb6593686006feb2aa13350c0664ec10e91fc58e0640fef8858f32c95。

每步都校验结果并从保存的命令重放继续；拒绝和重复命令必须保持状态哈希。输入字段、整数/布尔类型、坐标完整性、重复键和大小写错误严格拒绝。新增场景测试先取得5项未实现失败；标量/坐标严格校验也先取得真实失败再修复。当前Unity完整50/50通过；.NET在加入三份正式场景前的47/47已通过。

故意错误场景只完成准备一步，拒绝执行后续999金币命令；报告为失败，原生Player退出码1，修订1、金币0。该夹具保留在tests/fixtures，排除默认通过集。

可见截图：batch02-scenario-paused.png、batch02-scenario-complete.png、batch02-formal-scenario-complete.png。运行报告、最终状态、输入和程序集哈希均在artifacts/scenarios。图形展示不冒充真实鼠标/键盘测试。

## 第五阶段：小兵结算、推进与胜负

实现保护查询、无金币移除、2/4金击败、重型即时推进、独立累计标记、泉水/标记/水晶胜利、按正式新战区出生、队长选相邻空格及原阶段恢复。正式对局拒绝调试，胜后拒绝继续修改。原命令字段/枚举数值未改变；新增出生代数避免旧单位ID指向新兵。

测试夹具最初因公共Cmd辅助函数写死match而失败，修正为读取当前MatchId后，关闭新命令取得有效合同失败10/10，再接入实现取得10/10通过。随后增加满占位、出生争用、正式权限、场景与历史存档检查，Unity完整65/65通过（battlefield-full.xml），.NET在最后两项加入前63/63通过。

真实鼠标/键盘验收9项通过，包括错席位无合法格、2切队长、Home全图、点击远端出生格、确认恢复、金币不重复和第二次推进泉水获胜。发现并修复QA工具的Home扩展键标志及重启时读取旧布局的竞态；没有绕过地图命中。截图battlefield-ui-spawn.png、battlefield-ui-victory.png，报告battlefield-ui-1600x1000.json。

frontline场景7步的无窗口与可见最终状态哈希均为aa803e498b6473edb2864229598e11c409330bea7f2f1f9dec3c5f4486003dbd。BATCH-01旧59命令轮末存档已纳入固定回归，新增字段按默认值兼容，尚未改变旧RoundEnd行为。

满占位U-002保留出生任务，不自动删兵。多个出生点争用空格的先后U-012已向用户提问，当前明确暂停该局部情形；单点和无竞争出生正常可用。卡牌主要攻击尚未接入，不将调试击败证据归为卡牌完成。

## 第六阶段：补录已有出生顺序裁定

继续复核原历史时发现T-019eacb7-8d0a-7481-82d0-d92a6aa85d56第10条已规定非卡牌的跨队顺序由决策币颜色决定且不翻面，因此撤回U-012问题并补录D-023。先前提问选项中的翻面不是用户裁定，未采用。

实现队长从本队多个待出生小兵中任选先处理者，跨队按币色且不翻面。3项顺序测试先取得0/3失败，再实现并通过；Unity完整68/68通过，加入第5份正式场景后.NET69/69通过。Windows Player重建成功；spawn-order场景7步无窗口通过，实际窗口也已通过键盘2切队长、先点重型再选(3,-8)、后安排远程至(1,-7)并恢复暗选。结果11兵、金币0、币面不变，截图spawn-order-heavy-first.png、spawn-order-complete.png及spawn-order-ui.json。

引擎版本见ADR-003。新建局版本1；缺失版本按0重放，旧竞争出生暂停的结果仍可恢复。此举为解决重放兼容，不修改正式数据。U-002满占位仍保留；后续继续攻击、防御与英雄击败。

## 第七阶段：主要攻击、防御与英雄击败（13:04）

拔枪/快速突刺登记为带版本的三步卡程序；抵挡屏障/躲闪/挑战者登记独立防御语义。没有登记的主要牌文不省略文字执行普通攻击。开始后锁定动作，目标、攻击、防御、复活和重型出生都保存来源/控制者/游标；真实命令可从每个暂停处重放。公开攻击公式保留a+b-c明细，弃牌名称只投影给本人。

英雄击败按受害等级支付击杀/助攻、扣水晶并检查即时胜利；下一次行动先复活，再执行原揭示牌。等级1—8奖励与满占位均有核心检查；高等级奖励测试使用注明的局部状态夹具，不能当作真实升级证据。调试击败英雄已接入；响应期间阻止结构性调试改变局面。

数学4项、目标/防御查询5项分别先NotImplemented失败再通过；流程夹具最初因错误假设各英雄银牌先攻最低而失败，按正式牌面修正后取得7项有效unsupported_command失败，再接入执行器通过。新增重型暂停恢复、兵种奖励、满位复活、全部等级奖励、响应期间调试保护；保护查询先失败再修复。核心.NET101/101通过，补充两种主要抵挡及挑战者正负小兵修正后Unity完整104/104通过，报告combat-all.xml；不同报告计数不混合。

combat-defense正式场景17步无窗口通过，最终哈希c3390cfe75f79e16821a79fa28a29c75d6a80002b5a20580460084ae2bc1758c。新tools/test-combat-ui.ps1真实输入10项通过，截图combat-ui-attack.png、combat-ui-defeat.png、combat-ui-respawn.png；实际可见金色弃牌点、即时切席位及复活后的原牌。Windows Player已重新构建。

五张状态先提升到implemented并挂当前测试路径，尚不提升behavior_tested/integration_tested：持续免疫来源与到期尚未接入，完整交互矩阵和快速突刺的升级被动仍有待实现/验收。其余103张保持data_only。生成阅读视图改为读取独立状态，不再写死所有牌未实现。下一阶段继续轮末与升级闭环，本批尚未收尾。

## 第八阶段：轮末、多人升级与下一轮（13:35）

新增ResolveRoundEnd、ChooseRoundMinionRemoval、ChooseUpgrade三种显式命令，保留旧EndTurn历史结果。回收后一次快照兵数差，由少兵队长逐个移除；重型先推进/出生，随后恢复升级，旧差值不继续施加到新兵。即时胜利停止升级支出和补偿。旧普通确认59命令存档已实际通过第60条新命令继续下一轮并再次恢复。

每席自动按当前等级连续付费；升级选择可交错，候选仅本人可见。三色阶梯从真实升级记录推导；沙盒装配不伪造升级。未选候选图标按来源保存，六种加成各+1测试通过；8级紫卡单独授予，不进手牌。未升级者补偿在全部选择后发放，下一轮才能使用。快速突刺的选中/拒绝两分支永久加成已验收，状态仍implemented。

初始轮末8项均先取得有效未实现失败；接入后其中一项旧历史断言错误地期待删除全部历史，修正为“本轮无出牌但保留上轮16条”后8/8通过。新增升级7项，其中结构调试保护先失败再修复。场景升级断言先证明“命令接受但错误等级被漏检”，补查真实等级/紫卡/计数后通过。最终.NET与Unity均124/124，Unity报告round-upgrades-all.xml；资料11/11及内容/来源哈希通过。

实际窗口升级12项在1600×1000、1280×800均通过；轮末移兵—重型—出生—升级10项通过，包含暂停存读档。报告upgrade-ui-1600x1000.json、upgrade-ui-1280x800.json、round-minion-ui-1600x1000.json。截图upgrade-ui-candidates.png、upgrade-ui-next-round.png、round-ui-minion-choice.png、round-ui-birth.png、round-ui-next-round.png；同名升级截图以最后一次1280×800运行保留，两个JSON报告各自独立。

新增两份原生无窗口场景：round-upgrades 25步进入第5轮（含满级/紫卡及满级补偿），哈希e82e0c4454b829e107ffcb280f071f18eb5280d4396fe0d9749a289524711951；round-frontline 17步，哈希8c9bfaa47929ca0c8dc8244a113d0af0b16a069364d63ca999db369be53d9e2d。每一步均从保存的命令恢复后继续。

范围仍有明确边界：P-004紫卡只有拥有/展示，持续文字未执行；U-001轮末触发的精确交错留到相关效果；U-002满占位保持任务。本批继续代表牌与持续效果，尚未收尾。

## 第九阶段：持续效果、范围显示与存档能力升级（14:26）

静电封锁与打断施法通过版本2卡程序执行；来源卡/单位、控制者、创建顺序和生效/到期窗口保存进权威状态。封锁逐边过滤普通路径，快速移动另查起终点；动态中心与技能范围加成共享同一规则查询。技能禁止只作用于技能启动，攻击及防御不受误封。暗选、放弃和次要防御不会建立范围效果；回合结束删除来源并记录到期事件。

新增EngineUpgrade三项、EffectTimeline四项先取得未实装失败再通过；AuraFlow六项中四项先取得主要行动未实装失败，其余两项验证原有放弃/旧版本行为。几何六项覆盖绕圈/穿圈、快移、技能/远程加成、友方、无来源与独立来源；绕占位施法者的无效果对照最初预算少一格，修正为7步后通过，未把该夹具错误称为规则失败。额外三项先证明范围/永久数值投影缺失和场景断言漏检，再完成实现。最终共享核心.NET与Unity各148/148通过；资料11/11通过，正式内容及来源哈希未变。

界面主要动作按牌面类别命名；受禁技能关闭按钮并显示来源。右栏列出持续效果、到期时间及范围开关；紫色描边覆盖格来自核心，绿色仍表示合法目标。左侧升级永久数值公开，私有候选与加成来源仍仅本人可见。真实键鼠验收14项在1600×1000、1280×800各通过；1280×800升级流程12项回归通过，截图已目视确认来源、禁止提示及左侧加成可读。

十份原生场景共137步无窗口通过。新增static-field 13步、skill-suppression 12步均取得可见/无窗口相同最终状态：分别60febcbcdf889914d99d457d792eb77ebc001bd9f9cacba091dca2764a7a1d5c、c33a07c918dd1f582f8217e1874a1ca169eb8ae215a528bee61bcfe2efaf4c10。报告各自保留输入和程序集哈希；本阶段新状态字段使旧场景哈希变化，前述各阶段哈希是当时构建证据，不是跨版本固定值。

旧对局保持原引擎语义；全员未选牌且无剩余选择/效果的Planning阶段可显式UpgradeEngine，不修改初始版本。实际窗口从普通确认旧59命令文件结算至60，再点击“采用当前规则”保存为61条、InitialEngineVersion=0/EngineVersion=2，Sandbox仍为false。证据engine-upgrade-ui.json及前后截图。

当前两张范围技能按独立实现登记，合计7张implemented、101张data_only；免疫、取消来源与其他牌联动的完整矩阵尚未完成，不升为behavior_tested/integration_tested。艾瑞恩决斗家/传奇决斗家失败后免疫与同回合复活问题已单独提出U-013，等待用户裁定，只暂停该分支。本批按约12小时范围继续，尚未收尾。

## 第十阶段：闪耀之刃、来源取消与地图可读性

版本3加入闪耀之刃：相邻敌方英雄基础攻击3，防御成功仍继续取消相邻敌方技能的激活效果，再建立相邻封技。相邻范围显式独立于技能/远程范围加成；取消按实时位置、来源类别和激活窗口过滤，保留其他来源。取消事件包含被取消卡、发起牌与效果ID。无攻击目标不执行后续文字；次移/次防不触发主效果。

ShiningBlade六项中五项先取得能力/目标未实装失败，一项原有非主要使用与旧版本检查先通过；取消查询和相邻范围两项先失败再实现。新增真实版本2旧档回归与15步场景，最终.NET与Unity各158/158通过。固定夹具engine2-purple-aura.json在旧Player尚未重建时生成，15条真实命令包含满级紫卡拥有及基础技能后的一条范围效果，避免以后把新触发追加入旧记录。首次夹具输入误写夏尔加萨银牌ID，纠正为正式“海妖之歌”后生成通过文件，未当作规则失败。

十一份原生场景共152步通过无窗口运行；闪耀之刃可见/无窗口最终状态均85e94e160cb7ca82eab90318eea9bc7094cf2a8f44a5b6a19cab48ab57ca9de0。真实窗口持续效果脚本扩为三场景，1280×800共22项通过，含取消旧范围、建立新相邻范围与定位角色。数据校验11/11通过；状态负例修正为显式清空证据，不再错误假定数组第一张牌永远未实现。正式数据及来源哈希未变。

目视验收发现全图远景小兵文字挤在一起，并且额外范围提示占用地图高度。范围说明并入现有地图底栏；缩小时隐藏文字而保留单位标记，放大后恢复；加入“定位角色”将本人英雄居中并放至20像素六角半径，切席位仍保留视口。随后1280×800基础界面19项回归通过，覆盖鼠标锚点缩放、拖动/切席位、数字输入、地图命中及四边折叠。截图blade-ui-cancelled.png可见相邻两名英雄、范围边缘与取消后的来源。

当前8张implemented、100张data_only；尚不提高到完整行为/集成状态。电闪雷鸣“敌方一人有牌、一人空手时可否选空手者”已提出U-014，只暂停其合法目标筛选。接下来使用已明确裁定的屏障反制推进本人强制弃牌及防御后窗口；本批继续至约22:07的总体范围。

## 第十一阶段：屏障反制、本人弃牌与远程免疫

版本4开放偏转屏障与反射屏障。合法主要抵挡保存防御后记录，原攻击先完成后续指令；原攻击者有手牌时本人必须选一张弃置，空手自动继续，最后再结束父牌。反射屏障在该步骤之后创建本回合非相邻英雄远程攻击免疫；合法目标查询与攻击重检共用相同判断，其他行动类别不受扩大限制。

新增私有来源投影：弃牌候选只给弃牌者，防御来源只给原防御者；其他人仍可看到免疫事实、来源席位及到期。持续效果不伪造范围圈，也不把防御来源当成闪耀之刃可取消的技能来源。取消界面预选不会撤销权威强制选择。次要/快速移动不触发防御文字；升级选中/未选两分支准确应用另一张候选的图标。

首批6项在原实现取得有效失败后接入；补充非法/冒名/假跳过、两名攻击者、源类别、隐私、升级、第四回合及最后行动的反制边界，共24项屏障测试通过。双攻击者夹具曾误选先攻12的艾瑞恩银牌，导致预定的并列先攻不存在；按正式牌面修正为潮水后通过，不把夹具错误称作规则缺陷。最终Unity完整184/184，报告barriers-final.xml；.NET本阶段被Windows应用控制0x800711C7拦截，未绕过系统策略，使用同源Unity测试取得结果。资料11/11，正式内容/来源哈希未变。

Windows Player重新构建。十三份场景共193步无窗口通过；偏转屏障19步哈希1b4e787e6a67e7b63d3dbe4f11190f992b70cbc0309599c8d8e4f4723f526aaa；反射屏障22步可见/无窗口哈希均f68fa760a44a0a7ffa30a0c5d3f39225dd01aa2c3f87bdaffa902c6193ed6958。每步都恢复原命令后继续，拒绝与重复保持状态不变。

新增test-barriers-ui.ps1，真实键鼠16项在1280×800及1600×1000各通过，包括攻击/抵挡、本人选牌、取消不跳过、待弃牌存档关闭后恢复、不重复消费、左侧两个弃牌点、后续攻击排除黄蜂且允许其他敌人、回合结束清除免疫。截图barrier-ui-pending/resumed/protected；定位角色用于以可操作尺度显示目标，保留鼠标滚轮缩放。当前10张implemented、98张data_only；其他免疫及完整卡牌联动仍未全部验收，不提升为完整行为/集成状态。继续后续代表牌与工程整合，本批尚未收尾。

## 第十二阶段：已揭示攻击条件、数值明细与兼容夹具

版本5开放神枪手和一枪爆头，使用目标真实PlayedRound/PlayedTurn和攻击家族判断+2；基础攻击、未行动、已结算/放弃均覆盖，仅次防弃牌或过去回合不满足。神枪手排除相邻目标，一枪爆头允许相邻且仍为远程。牌文加成单独保存为CardTextBonus，显示与永久部分分开；挑战者忽略小兵项不会忽略该+2。

首批6项取得未实现失败后接入；相邻夹具最初只固定目标而未固定攻击者，被通用Duel布局差异影响，修正两者坐标后通过。新增基础攻击已放弃、合法时标/牌区、距离与两种加成、反射免疫、次防与次移/快速移动、四种升级图标，共33项专项通过。范围查询夹具进一步改为从正式地图选距离3的空格，2项重跑通过。场景攻击明细断言先证明缺失，再添加四项实际值检查；空攻击不能当0通过。完整Unity221/221，报告marksman-all.xml；.NET本机限制沿用前阶段说明。

在改变引擎5前，从6b800d9对应真实Player生成引擎4反射屏障待弃牌存档；固定夹具及输入/程序集哈希、产生时间均入库。新测试恢复其私有来源和旧能力，完成原攻击者弃牌，只产生一次攻击处理，并建立后续免疫。旧缺失CardTextBonus按0规范化，命令指纹未改。

Windows程序重新构建；十五份场景220步无窗口通过。神枪手13步哈希405e1795aa3163119e8c565c4bfa31946fe7664cc980b2207492c76ae206e422；一枪爆头14步可见/无窗口均f810c95bda0fd749ad66f8a6bf4e471351980665bcdd743bde71b48691298150。真实窗口test-marksman-ui.ps1在1280×800通过10项，截图marksman-ui-bonus可读攻击4＋牌文2、控物防御5不足6。公共UI助手抽取后屏障16项也在1280×800重新通过。资料11/11，正式内容哈希未变。

当前12张implemented、96张data_only。新增U-015自离出生点的空位判定时点、U-016放置自身能否原地两项具体问题，等待用户答复，仅影响艾瑞恩放置类目标。继续无该歧义的代表牌和工程工作，本批未收尾。

## 第十三阶段：相邻计数、背刺与加成来源

版本6开放劈砍、致命横扫、死亡回旋和背刺。前三张在攻击声明时以自身为中心计数所有相邻敌方英雄/三类小兵，包含目标及受保护重型，但选目标仍遵循重型保护。背刺以目标为中心检查其他友方英雄/小兵，明确排除攻击者，多个支援者只提供一次+2。普通小兵b/c项独立计算；挑战者只忽略小兵修正，不移除牌文加成。

首批8项在未开放能力时全部取得有效失败。最终39项专项包括单位种类/阵营/精确邻接、两种范围加成、来源及视图复制、无目标/攻击小兵、非法和重复命令、次移/快移/次防、升级两分支及旧版本。扩展夹具的距离2坐标、普通兵2金奖励、颜色键小写曾写错，按正式六角距离和资料修正后通过；未把夹具问题记为规则缺陷。加入4份场景及1份旧档回归，完整Unity265/265通过，报告adjacent-all.xml；资料11/11，正式内容与来源哈希未变。

在旧版本5 Player重建前取得真实一枪爆头待防御存档：engine5-headshot-pending.json有11条接受命令，包含已放弃基础攻击仍满足+2的结果。旧文件无新增原因/来源字段，恢复采用明确空默认，继续防御并于下一回合显式升级后仍能完整重放；输入和旧程序集哈希已记录。

Windows Player重建后，十九份原生无窗口场景285步通过。死亡回旋17步可见/无窗口最终状态同为5d79b8233b3aa2862ae8b007b135f623d9e629d628f2e2edbb5b4e09e45c817e。新增test-adjacent-attacks-ui.ps1在1280×800通过22项，实际点选四张牌的攻击、防御和后续行动。窗口列出数量×倍率、单位名和位置；cleave-ui-sources.png目视确认公式、五个来源与地图标记清晰可读，其他三张也分别保留截图。

当前16张implemented、92张data_only；没有提升为全部行为/集成通过。接下来继续非远程防御反制及其他独立工作，本批仍按约22:07收尾的范围推进。

## 第十四阶段：条件防御、空手反击与胜负显示

版本7开放近身格挡、近身还击、带头冲锋。防御条件描述符独立区分远程类别、最短距离和相邻友方小兵；“近身”牌名不擅自添加相邻条件。近身格挡空手继续，近身还击按牌文击败原攻击者；有手牌均由本人强制选弃。带头冲锋允许两种攻击类别，但必须有同队三类小兵之一相邻。不可抵挡攻击仍排除这些感叹号防御。

沿用攻击后文→防御后处理，闪耀之刃先完成自身持续文字，才处理攻击者弃牌或空手击败。反击死亡给虎爪及队友正确奖励、扣原攻击者队伍水晶；水晶归零直接结束比赛，防御后执行器不会把Finished重设为Action。私有防御的击败来源单独通过HeroDefeatSource记给原防御者，公开事件仍有受害者、位置和击败者，不能泄露完整防御牌。

首批6项取得未实装失败再通过；新增不可用原因投影的3项先失败再实现。最终43项专项覆盖攻击类型/相邻与远端、三类友兵/阵营/英雄与标记反例、强制/空手、冒名/非法/重复、私有来源、次移/快移/普通弃置、升级两分支，以及第四回合最后攻击被反击后进入轮末升级、下一轮复活并继续原牌。另加1份真实旧档回归及7份场景，完整Unity316/316，报告conditional-defense-final.xml。

场景初稿误用了不存在的EffectCount，已改为按来源的EffectCounts；另将累计事件误写为当前步骤EventCounts，并漏算艾瑞恩升级所得先攻+1，均修正场景期望后通过，没有据此改规则。二十六份原生无窗口场景395步通过；riposte-empty的26步可见/无窗口状态同为f6139f1a6f04ab889115957cbcd329a0184db9f396a4e6142d3eff0048c07997。

真实键鼠test-conditional-defenses-ui.ps1在1280×800完整28项通过。不可用原因只显示给防御者，手牌提示与合法按钮同源；普通数值不足仍能选并失败。目视发现右栏滚动可遮住胜者，于是固定顶部改显胜队；新增-Case可单选验收并保留独立报告，riposte-victory定向4项通过，截图确认“红队获胜”始终可见。原完整报告与补充报告分开保留，未用4项覆盖28项记录。

新版本6真实夹具只有近身还击一张剩余手牌，旧程序必须继续保留未实装防御窗口；重放验证不会追加入新反制。取证也发现版本4/5输入在Git索引被换行规范化而本地仍保有原CRLF字节，已用.gitattributes禁止历史夹具的换行转换，允许原CRLF并保留其他空白检查。重新暂存原文件后索引SHA-256与原取证记录相同，未重跑制造旧档。

资料11/11通过，正式内容及来源哈希未变。当前19张implemented、89张data_only；局部未决规则继续保留，不提高到全行为/集成状态。本批继续工程证据整理、长对局验证及后续独立内容，尚未收尾。

## 第十五阶段：历史夹具完整性检查

五份历史存档及四份输入加入manifest.json，校验原字节SHA-256、版本、命令数量、来源说明和登记完整性。测试先以八项失败验证缺失保护，再实现；追加的布尔false冒充旧引擎0负例揭示Python宽松相等问题，改为严格整数类型后通过。换行转换、语义等价的空白改写、路径越界、重复登记和漏登记均会失败。测试仅在已核对位置的临时副本执行，真实旧档保持不变。

资料及夹具工具合计20/20通过；本阶段没有修改游戏核心，沿用上一阶段Unity316/316结果，不冒充新跑核心。同步修正测试矩阵中仍停留在BATCH-01和强制弃牌未接入的旧描述。接下来生成可重放的长对局操作，检查阶段组合、升级和存档恢复。

## 第十六阶段：长对局模拟及版本7实物留存

新增固定种子的正式/沙盒模拟器，通过每个身份的合法查询产生真实命令。每步检查占位、五色牌、阶段收尾与公开投影；定期恢复完整历史，并探测冒名和重复命令。最终导出场景，由另一条ScenarioRunner路径重放并比较完整存档；这些是确定性和运行环境一致性测试，不替代手写逐卡规则断言。

初始四项对占位检查和未实现模拟器取得失败，随后短局4/4通过。版本7的101—108种子各运行400步，正式/沙盒交替，达到第8/11/8/10/8/10/8/10轮，合计3200条接受命令。实际触发40次攻击、22次防御结算、9次英雄击败及复活、12次战线推进、121次升级；本组未触发强制弃牌，也未完成胜负，均在步数上限停止。

首批9839b4d80ca442c19f51a480663141b8的八局生成及场景重放均一致，但各单项超过Unity默认180秒，最终XML为2/10通过、8项超时失败。已保留失败报告，不把生成结果写成NUnit通过。为长局单独设置一小时上限后，913462e191174adaae7620954304daff定向重跑种子101与两项短测试，3/3通过；原生程序400步结果同为a313cb6ecd18b29be0f8faf14d74a85af16d6e20f7fb851146a0d9ed93b6b528。

原八份输入另外完成原生后台重放，八局哈希均与各自生成结果一致；种子102可视化400步与后台同为9c1fd10cf339504b1840ec29098133efdff611b0bb2a1216b93407c22a1d919a。独立报告在原批player-replays/d3db47a290bc4f349e81cd4e5e3661c3，不覆盖超时结论。旧批启动时测试源码尚未提交，额外留存当时Editor已加载的五份程序集及哈希，避免用后续源文件冒充实际执行版本。

版本7 Player重建前，完整程序155文件留存在artifacts/player-history/engine7-448d541/player并登记哈希；同一旧程序产生11条真实命令的engine7-riposte-pending.json，仍在近身还击的强制弃牌中。固定夹具扩至六份存档/五份输入共11文件，新版恢复后只执行原反制一次，不能追加入之后的攻击前弃牌。全部历史回归7/7已通过。

## 第十七阶段：投掷牌的可选攻击前弃牌

版本8开放投掷飞斧与投掷长矛，共享本人可选弃牌→锁定条件距离→选择目标→攻击。飞斧只看本次实际支付，长矛看本人的Discarded区；“skip”与空手分支明确，不混用强制弃牌。没有目标时已支付成本保留。弃牌名本人可见、颜色公开，当前公开攻击源持续显示。反射屏障要求的另一张弃牌仍由原攻击者处理，不会重复支付攻击前成本或事后重算距离。

首批8项均取得primary_not_implemented失败后实现。最终43项投掷牌专项覆盖空手、已有弃牌、精确距离与两种永久图标、单位类别/阵营/重型保护/免疫、无目标成本保留、非法及冒名/重复、全选择窗口恢复、次移/快移/次防、升级未选图标，以及第四回合最后一张手牌支付后遭空手反射并回收进入新轮。扩展测试初稿误用了已出牌取回、屏障颜色和双未结算牌的非法查询夹具，已按正式规则修正，未据此改变引擎。场景初稿的累计/本步骤事件及duplicate结果期望也已纠正。

完整Unity367/367通过（optional-discard-final.xml）；二十九份原生后台场景442步通过。投掷飞斧/反射屏障20步可见及后台哈希均5ff1cd6d364ce7a5821b14f55d824e171df047bd367859db42b16e467cc3412f。真实键鼠1280×800共22项通过，截图确认距离3、高亮、四回合槽与实际弃牌点。首次小窗口脚本把可视区外目标当屏幕坐标点击，现先实际折叠顶部，并由公共Select-Cell检查地图可视边界；失败报告另存before-framing，未将它改写为通过。

当前21张implemented、87张data_only，正式6英雄/108牌/254格及内容哈希不变，未提升为全部行为/集成状态。

## 第十八阶段：可核验的本地程序包

Unity构建输出build-info.json，登记实际版本、能力、内容哈希、全部构建输入与155个程序文件的字节数及SHA-256。构建期间输入变化会失败；打包检查源文件集合/字节与程序集合/字节，避免把旧程序集配上新源代码。Python包工具先验证8项缺失保护失败再实现，扩展为11项通过；资料、历史夹具和包工具合计31/31通过。

实际程序包Goa2V1-e8-local-20260910-102508-fd30d3db.zip为47,226,666字节，外层SHA-256为1f95589c4603ffbf6e4e58ab029eee37a6e3f045e49c873225304c945632bf81；程序内容107,615,381字节、13个主要行动与8个防御绑定。包含清单，不包含测试程序集、存档或本机绝对路径。仅本地交付，没有上传或对外发布。

test-release-player.ps1先验证包再解压到新的独立目录，重新核对解压内容后启动包内exe，工作目录为解压目录，数据从包内StreamingAssets加载。实际后台20步通过，末态哈希与原生/可视化相同；证据在artifacts/release-smoke/0dcb3f79b1d0424f958914a8c2c9b1b5。工具支持-Visual，后续可用同一路径检查可见启动。报告与日志在程序目录外，包内容保持原样。

## 第十九阶段：调试局面与统一界面测试

新增八个真实命令预设：飞斧准备/攻击前弃牌/反射强制弃牌、长矛已有弃牌、近身还击、升级、英雄复活及占位出生。每次先运行并核验前缀场景，读取实际revision与选择者，再打开独立QA存档并按数字切至操作者。八个预设均通过后台准备；反射局面实际继续弃牌并完成一次反制，复活局面实际打开并切到第二席。未知预设和已有窗口未要求替换的两种入口均被正确拒绝；原正常存档未变。

架构、里程碑、下一批计划与开发流程已按当前代码更新，清除战斗/轮末仍未开始等旧状态。统一test-ui-suite.ps1逐模块保存新报告、截图及前后程序哈希；战场模块在1280×800真实9项通过，报告78a1a8b296f341ac95a1c01694b9ce86。初版时间戳检查将已解析DateTime再次转显示文本而丢失小数，造成误报；改为保留原精度，初版失败证据bd0aa6ccfd42405bbeaacffc13920b9f另存。当前不把单模块通过写成整套通过。

小窗口1152×768暴露调试传送按钮中心被裁切而快照仍报可见的问题，前14项通过后地图选择未生效；手工滚动后同一按钮可用且产生194个合法格。原报告/截图另存before-hit-test-fix。界面命中判断和揭示牌溢出修复待当前长局完成后构建验收。

## 第二十阶段：长存档性能与隔离重放恢复

700条命令、838,936字节的第18轮沙盒存档，实际可见程序完整启动至恢复界面初测85.23秒。独立measure-player-load.ps1按固定15秒预算重测84.23秒、进程CPU82.19秒，正确返回超预算失败；报告before-replay-fc9c202176f446368d3e0961bd081c7f保存实际输入及程序集哈希。存档SHA-256为95634758744984abb5d1293d433e017809264053ae748c06e07b040dc425dd74。

调查发现恢复逐条调用面向实时交互的Execute，为每个无人读取的结果生成完整视图及重复副本。计划在不可见的独立会话中重放全部命令，保留同一校验/回执/事件路径，成功并通过完整状态比较后才返回会话；实时命令继续保持原有非法命令原子性。当前长局仍使用原程序，性能实现与前后比较后续单独记录。

上述计划已经实现。ReplayImport先以9/9未实现失败验证入口缺失，随后专项及完整核心376/376通过。新工厂只在尚未返回给外部的会话内省略中间视图及重复整局复制；任何身份、命令或规则拒绝均放弃整份恢复。LocalGameFactory仍严格校验版本/资料，从规范初始状态重放，最后比较完整保存结果。规则/引擎/协议版本与输入哈希均不改变，实时Execute继续原子执行。

同一700条存档在重建程序中完整启动、恢复与首次快照耗时5.67秒、CPU2.36秒，通过15秒预算；报告after-replay-7977f5a55f874d2b99a81dc93ac01ddb。优化前完整程序单独冻结于artifacts/player-history/engine8-before-replay，未覆盖。测量包含程序和界面启动，只说明本机这份输入的端到端结果，具体决策见ADR-004。

四组引擎8长局5617f24c65e647518aec2a9eab3f2dcb共2321条接受命令，长模拟与两项小测试合计6/6通过。种子201正式局468步，第9轮红队自然水晶胜利；203正式局453步，第9轮蓝队自然水晶胜利；202/204沙盒局分别700步，第18轮因步数上限停止。四组原程序后台重放通过，随后优化后程序再次重放，四份完整末态分别与原结果相同；新证据在player-replays/fe6b0cae5bdc4d0fb5d96162c48429d3。两局达到上限没有写成获胜完成。

## 第二十一阶段：游戏内预设与小窗口修正

八份预设随构建从正式场景前缀生成到StreamingAssets/Goa2Debug。游戏内“调试→打开测试局面…”先完整验证新局面，再保存旧局，最后切换至待选择者/行动者；取消不改旧对局，弹窗屏蔽背后席位快捷键。正式对局和自动场景播放不能使用调试预设。每次局面使用独立对局ID，仍由普通命令构建和完整恢复，没有直接注入最终状态。

新增DebugPosition18项，包括八份实际阶段、恢复、独立会话、错误期望/最终阶段拒绝、格式/权限/版本/重复ID与来源路径校验；最终完整Unity394/394通过，报告native-presets-all.xml。预设元数据及场景源纳入构建输入，新增一项包工具回归，Python合计32/32通过。当前Player二十九份后台场景442步通过。

1152×768基础操作19项通过。实际截图促成手牌名称/主要数值/次要数值/状态分行，卡片溢出裁剪；主数值在窄卡缩至14号，普通文本17号、牌名19号，其他放大后的面板字体保留。角色快捷切换增加单次滚动，使选中角色进入左栏可视区；普通刷新保留用户滚动，地图视角和折叠状态不变。原“第四角色在可视区外”失败保存在486bbfa3ca6840ebac6e434f080ce953，修复后预设真实键鼠26/26通过，证据4c8d5421524c4987a0b238b9da62d91e。

小窗口smoke/presets/optional-discard三模块原65项通过，报告77a8f4a2b2844724b5f51a3bcfc7a7d3（预设当时24项，随后追加两项角色滚动检查）。快照替换期间读取文件时间的竞争曾使70a96365d8ea46d4aca27537d605e408失败；脚本把快照和时间读取放入同一重试段后通过，失败证据保留。正在继续完整12模块和独立源码构建验收。

新版程序包Goa2V1-e8-batch2-20260910-121956-90d19eb7.zip包含156个程序内容文件、107,756,587字节，SHA-256为57a5a1a2076dc104bb56add3b24462b996693efb6a7f85eb78eadd792ab020e7。独立解压后台20步通过，末态仍为5ff1cd6d364ce7a5821b14f55d824e171df047bd367859db42b16e467cc3412f；证据artifacts/release-smoke/1462e66094034961a76f8d26f85ab43a。旧包保留。

## 第二十二阶段：独立源码、完整界面与长局继续

12个真实窗口模块在1280×800完整207/207通过，运行期间全部程序文件哈希保持一致；证据artifacts/ui-suites/7374d2bb116c40fa949630ec902a7c05。上阶段程序包独立解压可见20步也通过，证据artifacts/release-smoke/998af37a07bb4b9eb784d5f41163f066，末态与后台相同。

新增test-source-checkout.ps1，从指定本地提交导出Git源码到独立新目录，检查不含Library或预生成内容，再运行资料校验、可选完整Unity测试、Windows构建和包内场景。提交2f9e196首次验证资料32项、Unity394项、构建和20步场景全部通过；证据artifacts/source-checkouts/80979c6befa64169a280fda53be6d543。它使用本机已安装Editor及全局包缓存，只证明新路径与项目缓存可重建，不声称在全新电脑完成安装。

优化后程序可见播放种子202的700步，末态仍为95634758744984abb5d1293d433e017809264053ae748c06e07b040dc425dd74，证据原长局批内player-replays/d22f1e192076462ba173de62b4c28e38。test-long-ui.ps1另在1152×768恢复该档，八次真实角色切换均保持地图并让当前角色滚入可视区，实际加5金后关闭和恢复完整701条存档，22/22通过。八次响应约508—1033毫秒，加金约755毫秒，均低于1500毫秒预算；包括输入工具固定350毫秒等待，不写成纯规则耗时。原700条输入不变，证据artifacts/long-ui/b40f5260217848ceae3f3bf1adedbfad。

新增单文件离线资料浏览器，内嵌108张卡、254格地图、21份已接入合同及规则/裁定/疑问/复盘/操作说明，登记34份输入哈希。卡牌全文/颜色/状态过滤、完整详情、地图坐标与区域、拖动与滚轮、规则段落检索均不改正式资料，不发网络请求。实际浏览器曾暴露窗口改尺寸后缩放锚点错误，失败证据07accb7271584984b9088876b28fc80f；改用SVG实际坐标矩阵后23/23通过，证据artifacts/reference-tests/57f376d675de420a90d8560e17e91d91。先前模拟鼠标坐标舍入导致的测试假设错误另行修正，没有据此改规则。

四身份网络整理为ADR-005与NET-01验收设计：真实四客户端、独立身份绑定、本人投影、重复/过期/续连、16项进程案例、服务重启承诺独立门槛。状态为设计，未创建网络服务或联网入口。

## 第二十三阶段：读取故障恢复与游戏内图鉴检索

test-recovery-ui.ps1先在旧程序确认保存失败没有可见说明、启动坏存档缺少恢复动作，原失败分别保留在artifacts/recovery-ui/9e37ae2d66c94052a89660640d5b9ec7与5bebd7baa92a41e780335a1c4a3a30d1。新程序启动失败页可重试已修复存档、创建测试对局或退出；保存新对局失败会停留原局并显示重试说明。只有整份输入校验/重放成功才切换会话。

独立QA副本注入坏JSON、伪造金币、未来引擎、错误内容、缺失文件和保存临时路径阻塞；19/19在1152×768通过。故障源文件保留，未修改系统权限、正常用户存档或绕过验证。13模块整套1280×800进一步226/226通过，证据artifacts/ui-suites/1d4f304042f24b048c15188616ef2eca。程序资料缺失分支提供错误页，但本阶段未对其做实际故障注入，不把它计作已验证路径。

图鉴加入全英雄108牌、名称/原文/英雄/牌号检索、本局可用筛选和明确空结果。输入更新仅过滤现有卡片，保留焦点和光标；按数字输入不会触发席位切换。以旧Player首次运行新脚本取得缺搜索栏失败，保存在artifacts/unity/gallery-ui-before-search.json；新Player的1152×768真实Unicode键盘12/12通过，包括当前21张、旧引擎0的5张及全部浏览操作后的完整对局不变。原生代码提交12c1e05后冻结功能，最后执行14模块回归与本地交付核验。

## 第二十四阶段：最终验收与交付归档

最终14模块真实键鼠在1280×800和1152×768各238/238通过，程序文件前后完全一致。两份报告分别为artifacts/ui-suites/9c7cfe75028f468983e8ad53f45ac38f与7b5f1cdef43c41d2acdc4c586852e7ea。最小窗口复核涵盖所有代表攻防窗口，没有仅用基础冒烟代替完整交互验收。

提交1f31f70再次从Git导出到无Library、无预生成资料的新目录，Python32/32、Unity394/394、Windows构建和20步原生场景通过；证据artifacts/source-checkouts/ab00eed10f3c43e9bfe839d10e6da1b2。仍使用本机已安装工具和全局包缓存，明确不声称全新机器安装验收。

最终程序包Goa2V1-e8-final-20260910-131348-72e26372.zip为47,239,426字节，SHA-256为489dc36c3420ad77e9d9986f5163f028f652ca34e4af96ca047a648ab0c9f5c9；156个内容文件、107,762,919字节、188份构建输入。独立解压后台和可见20步均通过，末态5ff1cd6d364ce7a5821b14f55d824e171df047bd367859db42b16e467cc3412f；证据8857d7f0cd014d689390f9047357a5ba与91a4fc52355f445b9e137c1c054f2f62。

另在独立解压副本暂存卡数据，实际资料缺失页显示中文原因和退出按钮，不提供无效的新局/重试存档动作、不写替代存档。恢复原文件后全量包内容核验及四席正常启动通过；test-release-content-ui.ps1共7/7，证据artifacts/release-content-ui/30e0ed73a9274abda12cd0ec06999b7c。第二十三阶段提到尚未注入的程序资料缺失分支至此取得实际证据；主程序和原压缩包均未改。

可见播放控制追加独立12/12真实键鼠：暂停起步、切席观察、手工命令隔离、单步、继续/暂停、完成后转手工并加金保存，原场景和已核验最终存档保持原字节。9步末态与后台同为e9796cb37f4b13d1e409bae937b5a79d15e84c58deb337714b786bd69009529f；证据artifacts/playback-ui/92fa26e1750b4585a936618d4c96f7be。初稿错误使用英雄简称匹配全称按钮、全称匹配简称确认，以及读取前次启动的旧快照；失败58f6040fe0ff4494a228cd3ccfae664c、dfba7c37fa0a472893b625efd15fe1c6、ea517a3129e34e4db1d8f60651db74f0保留。改为正式名称与当前确认文字、等待本次新快照后通过；没有为这些脚本假设修改游戏规则。

离线资料浏览器按最终操作说明重新生成，可见浏览器23/23通过，实际无网络请求和脚本错误；证据artifacts/reference-tests/d5ac34267cc84425aa3c9b607a4bb782。HTML哈希ea91049681b35e2ae046b2a3f68d022c690ad37a8cee9800cb579e86c45796e5，34份输入、108牌、254格。当前原生窗口和离线目录截图已目视确认并保留到验收文档。

87张未接入牌逐一核对到14个依赖组，无重无漏且不含21张已接入牌。下一批先按偷袭→魅惑建立动作内移动和完整主要移动，再插入NET-01四身份切片，其他牌族按依赖交错。此为开发路线，不把准备稿或未回复裁定升级为已定合同。同步清除持续效果说明和ADR中仍写当前引擎3/7的旧状态，当前均为8。
